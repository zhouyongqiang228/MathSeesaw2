# Math Seesaw（数学跷跷板 / PutMan）完整复刻文档

> 本文档用于在一个**全新的、干净的 Unity 工程**中，从零完整复刻 `l_MathSeesaw` 这一款游戏。
> 文档不依赖原工程的任何其它系统（广告、GA、关卡合集框架等都已剥离），只描述本游戏自身需要的最小完整内容。
> 你可以把本文档整篇交给 AI，让它据此搭建工程。

---

## 0. 一句话玩法

屏幕中央有一个**跷跷板（天平）**，左右两个托盘。屏幕下方/旁边散落着若干**写着数字的小人**。
玩家把每个小人拖到左盘或右盘的座位上。当**左盘所有数字之和 == 右盘所有数字之和**（且所有该放的人都已放好）时，跷跷板水平平衡 → **过关**。

数字越大的一侧越重，跷跷板向那一侧倾斜。玩家通过分配小人让两边相等。

进阶机制：
- 有些座位带 **倍率 Buff**（例如 `×2`、`×3`、`×0`），放上去的小人数字会被乘以该倍率再计入该侧重量。
- 有些小人**数字被隐藏**，显示为 `?` 或字母（`A/B/C…`），需要玩家推理。
- 有些小人**一开始就固定站在跷跷板上**（不可移动），作为已知配重。
- 关卡可换不同**皮肤**（环境材质 + 摄像机背景色 + 角色材质 + 天空盒）。

---

## 1. 工程总体结构

### 1.1 场景层级（建议）

```
Scene "Game"
├── Main Camera            (Camera, 透视, 由皮肤控制 clearFlags/背景色)
├── Directional Light      (由关卡 LevelBase 控制角度/强度)
├── Battle (空物体, 挂 Battle.cs)         ← 游戏总控/关卡加载器
│   └── (运行时在此实例化当前关卡 Prefab)
├── BattleRes (空物体, 挂 BattleRes.cs)   ← 皮肤/材质库 (单例)
├── Canvas_Battle (UGUI Canvas)           ← 顶部关卡文字、Replay 按钮等
│   ├── Text "Level X"
│   └── Button Replay
└── EventSystem
```

每个**关卡是一个 Prefab**（`L_1` ~ `L_46`），运行时根据当前关卡号从 `Resources/l_MathSeesaw/L_{n}` 加载并实例化为 `Battle` 的子物体。

### 1.2 关卡 Prefab 内部层级

```
L_n (root, 挂 Level_PutMan.cs : LevelBase)
└── AllPutMan (挂 All_MathSeesaw.cs)
    ├── Cube                      (环境/地面 MeshRenderer, 应用皮肤 env 材质)
    ├── Clouds                    (装饰云, 皮肤可关闭)
    ├── putMans                   (所有"可拖动小人"的父节点)
    │   ├── PutMan (0)  …          (每个挂 PutMan.cs, 预放 8 个, 按需启用)
    │   └── … (共 8 个)
    ├── Seesaw (挂 Blance.cs)      ← 跷跷板本体
    │   └── up (m_upComponent, 会绕 Z 轴旋转的横梁)
    │       ├── LeftPan  (挂 NumContainer_PutMan.cs)
    │       │   ├── 总分 TextMesh
    │       │   ├── ManPlace 0..3 (左盘 4 个座位, 各挂 ManPlace.cs)
    │       │   └── bg collider (tag "ci")
    │       └── RightPan (挂 NumContainer_PutMan.cs)
    │           ├── 总分 TextMesh
    │           ├── ManPlace 0..3 (右盘 4 个座位)
    │           └── bg collider (tag "ci")
    └── Cav_MathSeesaw (UI, 挂 Cav_MathSeesawUI.cs)
        └── LevelProgress (挂 LevelProgress.cs, 顶部 1-2-3-4-Boss 进度条)
```

> 关卡内还有 `movePos1` / `movePos2`（教学引导手指的移动路径锚点）和 `Cav_Tutorial`（仅第 1 关左右的新手引导），属于可选项，复刻时可后补。

---

## 2. 核心数据结构与算法

### 2.1 数字小人 `PutMan`

挂在每个可拖动小人上。

字段：
- `MyManModule m_manModule`：角色模型 + 动画（含 `m_meshRender` 用于换肤材质）。
- `TextMesh m_textNum`：显示在小人身上的数字。
- `Transform m_massPoint`：质心点（可选）。
- `int m_iInitNum`：该小人的原始数字。
- `bool m_bHideNum`：是否隐藏数字。
- `ManPlace m_curManPlace`：当前所在座位（null = 在散落区）。
- `PutManBuff m_buf`：当前座位施加的 buff（影响显示数字）。

关键方法：
- `myInit(int num, bool bHideNum)`：设置数字和是否隐藏，刷新显示。
- `getScore()`：`m_buf == null ? m_iInitNum : m_buf.m_bufData.calcu(m_iInitNum)`。即**最终计入重量的值 = buff 作用于原始数字后的结果**。
- `setText(score)`：
  - 若 `m_bHideNum`：显示 `Level_PutMan.GetLettersByNumber(score)`（useLetter 模式下显示字母，否则 `?`）。
  - 否则显示数字字符串。
  - 文字缩放随数值位数变化：`>=100 → 0.006`，`>=10 → 0.008`，否则 `0.01`（localScale 等比）。
- `onPutToPlace(ManPlace)`：放到座位时绑定该座位的 buff；若原座位不同则清空原座位；如果开启 `m_bIsSet`（静态开关，默认 false）会播放"坐下"动画并旋转朝向。
- `saveInitState()/restoreInitSate()`：拖拽失败时回到散落区的初始位置/父节点/缩放。

### 2.2 座位 `ManPlace`

挂在每个托盘座位上。

字段：
- `NumContainer_PutMan m_NumContainer`：所属托盘。
- `PutManBuff m_buf`：该座位的倍率 buff 组件。
- `Transform m_standCylinder`：座位底座。
- `GameObject m_objBang`：被"钉死"的标记（站着的固定小人用）。
- `bool m_bIsLeft`：是否左盘座位。
- `bool m_isbang`：是否被锁定（固定小人占据，不可再拖动）。
- `Transform m_parentMan`：小人放进来后挂载的父节点。
- `PutMan m_puttedMan`：当前占据该座位的小人。
- `GameObject m_alphaMan`：半透明预览小人（拖拽时提示落点）。

关键方法：
- `setMan(PutMan)`：把小人放入座位（设父节点、归零本地坐标/角度、触发 buff 绑定、轻震动）；若座位已被别的占据返回 false。
- `getScore()`：返回所放小人的 `getScore()`，空座位返回 0。
- `showAlphaMan(bool)`：拖拽时显示/隐藏半透明落点提示。
- `setBang(bool)`：设置固定锁定状态并显示锁定标记。

### 2.3 托盘 `NumContainer_PutMan : NumContainer_Base`

字段：
- `int m_iInitScore`：托盘初始固定分（一般 0）。
- `int m_iTotalScore`：托盘当前总分。
- `TextMesh m_textNum`：显示总分。
- `List<ManPlace> m_vManPlaces`：本托盘所有座位（4 个）。

关键方法：
- `updateTotalScore(bool forceShow=false)`：`总分 = m_iInitScore + Σ 各座位 getScore()`；若有任意座位的小人数字被隐藏，则总分文本显示 `?`（除非 forceShow）。
- `getEmptyPutManPlace(Vector3 hitPos)`：返回离命中点最近的空座位（拖拽吸附用）。
- `onGameWin()`：强制显示真实总分，并让所有在座小人揭示真实数字。

### 2.4 跷跷板 `Blance`

字段：
- `Transform m_upComponent`：会旋转的横梁（绕本地 Z 轴）。
- `NumContainer_Base m_leftNumContainer / m_rightNumContainer`。
- `int m_leftWeight / m_rightWeight`：左右当前重量。
- `bool m_bAwlayMaxAngle`：是否只用"最大角"两档（true 时不平衡就直接到 ±最大角）。
- `float m_targetAngel`：目标角度。
- 静态：`m_fMaxAngel = 13`（最大倾斜 13°），`m_fAngelScale = 1`（每 1% 差距对应 1°×scale）。

角度算法 `getAngleByWeight(left,right)`：
```
if left==0 && right==0 → 0
if m_bAwlayMaxAngle:  left==right→0; left<right→-max; else +max
否则:
  max=Max(left,right); min=Min(left,right)
  percent = (min>0) ? (max-min)/min*100 : 100
  percent *= m_fAngelScale
  clamp 到 m_fMaxAngel
  if left<right → percent = -percent
  return percent   // 正值=左重(右翘起), 用作 up 横梁的 Z 欧拉角
```

`updateWeight(left,right,immediate=false)`：
- 若重量没变直接返回。
- 记录新重量、算目标角，`startGoTargetAngel`。
- 动画：`m_upComponent.DORotate((0,0,targetAngel), 1.5s)`（用 DOTween），完成后 `onRotateOver()`。
- `immediate=true` 时直接设角度并立刻判定（用于关卡初始化时把"已站立小人"的重量瞬间生效）。

`onRotateOver()`：旋转动画结束时调用当前关卡类型的胜负检查（本游戏即 `Level_PutMan.checkAndDealGameOver()`）。

适配：iPhoneX 机型 `transform.localScale = (0.8,0.8,1)`，否则 `(1,1,1)`。

### 2.5 倍率 Buff `PutManBuff`

结构体 `stBufData`：
- `char m_op`：运算符。`'0'`=空(无 buff)，`'+'`=加，`'-'`=减，`'X'`=乘（注意 `'x'` 会被 normalize 成 `'X'`）。
- `int m_iNum`：运算数。
- `calcu(int num)`：`+`→num+iNum，`-`→num-iNum，`X`→num*iNum，否则原样返回。

> 在 Unity 序列化中 `char` 存为数值。本游戏所有关卡的 buff 都是 `m_op: 88`（ASCII 88 = `'X'` = **乘法**）。例如 `{op:'X', num:2}` 即"该座位把放上的小人数字 ×2"。

显示：组件上的 `TextMesh m_testNum` 显示 `"X2"` 之类；空 buff 时 `gameObject.SetActive(false)` 隐藏。

### 2.6 关卡主控 `Level_PutMan : LevelBase`

这是每个关卡 Prefab 根节点的脚本，承载该关卡的**全部可配置数据**。

静态：
- `m_iCountOfManPlaceInLeft = 4`：每个托盘 4 个座位。
- Tag：`Tag_PutManPlace = "ball"`（座位碰撞体 tag），`Tag_PutManBg = "ci"`（托盘背景 tag）。

组件引用：
- `Blance m_blance`：跷跷板。
- 运行时取得 `m_leftNumContainer / m_rightNumContainer`（= `m_blance` 的左右托盘）。
- `All_MathSeesaw m_all`：通过遍历子物体获取（用于拿到 `m_all.m_ui.m_levelProgress`）。

**关卡配置字段（这些就是每关要在 Inspector 填的全部数据）：**
- `int m_skinIndex`：皮肤索引（见第 4 节）。`-1` 表示用最后一个皮肤。
- `bool m_GeneRandomPutMan`：是否程序随机生成本关数据（true 则忽略下面手填的数字，按算法随机，见 2.7）。
- `bool m_bUseLetter`：隐藏数字时显示字母（true）还是 `?`（false）。
- `int m_iInitPutManNum`：本关**可拖动**小人的数量（启用 putMans 的前 N 个）。
- `int[] m_vStandInSeesawLeft`：开局就**固定站在左盘**的小人数字（不可拖动）。
- `int[] m_vStandInSeesawRight`：开局固定站在右盘的小人数字。
- `int[] m_vLeft`、`int[] m_vRight`：**可拖动小人的数字池**。注意 `vLeft` 和 `vRight` 在运行时会被**合并**成一个池 `m_vNums = merge(vLeft, vRight)`；它们只是设计者录入时的分组（通常代表一个标准解：vLeft 之和 == vRight 之和），并**不强制**小人必须放左/右。
- `bool[] m_vIsHide_StandInSeesawLeft`、`m_vIsHide_StandInSeesawRight`：上述固定站立小人是否隐藏数字（与各自数组一一对应）。
- `bool[] m_vIsHide`：可拖动小人是否隐藏数字（长度对应 `m_vNums`；为空则全不隐藏）。
- `bool m_bRandomTheLeftAndRight`：true=把数字池随机打乱顺序分配给小人；false=按从大到小排序后分配。
- `List<stBufData> m_vLeftBuf`、`m_vRigtBuf`：左/右盘各座位的 buff 列表（按座位顺序）。空表示无 buff。

继承自 `LevelBase` 的通用字段（本游戏关卡的典型值，见第 5 节）：
`m_eLevelType = PutMan(=1)`，`m_iDiffcultDiv`，`m_bShowLvlText=false`（本游戏关掉了关卡文字，用顶部进度条代替）等。

**初始化流程 `Start()`：**
1. `m_bShowLvlText = false`；`initGlobalSetting()`（应用 LevelBase 的相机/灯光/UI 设置）。
2. `m_all.m_ui.m_levelProgress.setCurLevel(当前关卡号)`：刷新顶部进度条。
3. 三个 `m_vIsHide*` 若为空则按对应长度初始化为全 false。
4. 取左右托盘引用。
5. 若 `m_GeneRandomPutMan` → `geneRandomData()` 用算法生成 `m_vLeft/m_vRight`。
6. `m_vNums = merge(m_vLeft, m_vRight)`；按 `m_bRandomTheLeftAndRight` 打乱或降序排序（同步重排 `m_vIsHide`）。
7. `initPutMan()`：启用前 `m_iInitPutManNum` 个小人，逐个 `myInit(数字, 是否隐藏)`；其余隐藏。初始化每个座位的 buff（`_initBufs`，左盘 buff 列表会 `Reverse()`）。
8. `updateScore()`：计算两盘总分并刷新跷跷板角度。
9. `initSkin()`：按 `m_skinIndex` 取皮肤，设置相机 clearFlags/背景色或天空盒、Cube 环境材质、每个小人的角色材质、是否显示云。
10. `initStandSeesawMan()`：把固定站立小人放到对应座位并 `setBang(true)` 锁定；用 `m_blance.updateWeight(leftSum,rightSum, immediate=true)` 立即生效初始重量。

> 站立小人取用的是 `m_vPutMan` 列表**末尾**的小人对象（从后往前 `j--`），左盘从 index 0 起的座位、右盘从末位座位往前放。

**输入/拖拽 `Update()`（核心交互）：**
- 射线层约定：
  - Layer 15：可点击的小人（`PutMan` 的碰撞体）。
  - Layer 9：拖拽时跟手的"背景平面"（小人跟随命中点移动）。
  - Layer 8：座位/托盘背景（用于吸附到最近空座位，tag 区分 `ball` 座位 / `ci` 托盘背景）。
- 按下：射线打到小人（且其所在座位未被 bang 锁定）→ 选中，调用 `man.onPutToPlace(null)`（从原座位脱离），轻震动。
- 拖动：小人跟随 Layer9 平面命中点；同时射线找最近可放置座位 `getTouchedPlace()` 并显示半透明落点提示；若点到已占用座位或托盘背景，则换成该托盘里离命中点最近的空座位。
- 抬手：若有目标空座位则 `setMan`；否则 `restoreInitSate()` 回到散落区。然后 `updateScore()` 刷新天平。

**胜负判定 `checkAndDealGameOver()`（由 Blance 旋转结束时回调）：**
```
if 左重量 != 右重量 → return（未平衡）
for i in [0, m_iInitPutManNum): 若有任一可拖动小人还没放进座位 → return
否则 → MyData.m_battle.setGameWin(); onGameWin()（揭示所有隐藏数字）
```
即**胜利条件 = 左右配重相等 且 所有可拖动小人都已放置**。

**隐藏数字→字母映射 `GetLettersByNumber(int)`：**
- `m_bUseLetter == false` → 返回 `"?"`。
- 否则逐位把数字映射成字母：第一次遇到的数字位分配 `A`，下一个新数字位 `B`……（即相同数字 → 相同字母，多位数逐位拼接）。用于"找出哪些字母代表相同/不同数值"的推理玩法。

### 2.7 随机关卡生成 `geneRandomData()`（仅 `m_GeneRandomPutMan=true` 用，如 L_46）

```
seed = 当前关卡号; Random.seed = seed
level = 当前关卡的 m_iLevelDiff
小人数 m_iInitPutManNum:
  level==1 → 3
  level<5  → 4
  level<10 → 5
  level<20 → Random[5,7)
  否则     → Random[5,9)
随机切分左右人数 l,r（各 1..4，且和=总人数）→ left=min, right=max
每人分数 = Random%level + 1; 分别求 sumLeft,sumRight
保证 sumLeft>=sumRight（不够则交换两侧）
然后不断给较小侧随机加值，直到 sumLeft==sumRight（保证有解、两边相等）
结果写入 m_vLeft / m_vRight
```

---

## 3. 顶部进度条 `LevelProgress`

UI 脚本（用 TextMeshPro + Image）：
- `TextMeshProUGUI[5] m_textLevels`：显示 5 个节点数字（每 5 关一组，第 5 个固定写 `"Boss"`）。
- `Image m_imgProgress`：进度条填充（`fillAmount`）。
- `Image[] m_imgArrow`：当前关卡指示箭头。
- 进度档位：`{0, 0.121, 0.322, 0.523, 0.724}`（对应一组内第 1..5 关）。
- 当前关高亮色：`#00E6FF`（RGB 0,0.9,1）。

`setCurLevel(int level)`：
```
startLevel = (level-1)/5*5 + 1            // 本组起始关
progress   = m_vProgress[(level-1)%5]     // 组内进度
for i in 0..4:
  cur = startLevel + i
  text = (i==4) ? "Boss" : cur.ToString()
  若 cur==level: 显示该箭头, 文字染高亮色
  否则: 文字白色
m_imgProgress.fillAmount = progress
```

---

## 4. 皮肤系统

### 4.1 `MathSeesawSkinData`（每个皮肤的序列化配置）
- `int m_skyMaterialIndex`：天空盒材质索引，`-1` 表示不用天空盒（改用纯色背景）。
- `Color cameraColor`：相机纯色背景（无天空盒时使用）。
- `bool showCloud`：是否显示装饰云。
- `int m_envMaterialIndex`：环境/地面 Cube 材质索引。
- `int m_roleMaterialIndex`：角色材质索引；`-1` 表示不替换角色材质。

### 4.2 `BattleRes`（单例，挂在场景里）
- `MathSeesawSkinData[] m_vMathSeesawSkinData`：皮肤库（本游戏共 **11 个**，索引 0..10）。
- `Material[] m_vMaterial_role`：角色材质库。
- 取皮肤：`GetMathSeesawSkin(index)` 用 `(index + len) % len` 取（所以 `-1` → 索引 10）。
- 材质从 `Resources/Mats/` 加载：环境 `env_{i}.mat`，天空盒 `sky_{i}.mat`。

### 4.3 皮肤库具体数值（务必照抄）

| skinIndex | sky | cameraColor (RGB) | showCloud | envMat | roleMat |
|---|---|---|---|---|---|
| 0 | 无(-1) | (0.4235, 0.8392, 0.9961) 浅蓝 | 是 | env_0 | role0 |
| 1 | 无 | (0.5843, 0.8824, 0.8275) 薄荷绿 | 是 | env_4 | role0 |
| 2 | 无 | (0.2118, 0.3098, 0.4196) 深蓝 | 是 | env_5 | role0 |
| 3 | 无 | (0.3216, 0.0706, 0.3843) 紫 | 是 | env_6 | role0 |
| 4 | 无 | (0.6510, 1.0, 0.9490) 青 | 是 | env_7 | role0 |
| 5 | 无 | (0.2235, 0.1961, 0.1961) 暗灰 | 是 | env_8 | role0 |
| 6 | 无 | (0.4235, 0.8392, 0.9961) 浅蓝 | 是 | env_9 | role0 |
| 7 | 无 | (0.9686, 0.9569, 0.5451) 黄 | 是 | env_10 | role0 |
| 8 | sky_3 | (0.2, 0.2, 0.2) | 否 | env_12 | role1 |
| 9 | sky_2 | (0.2, 0.2, 0.2) | 否 | env_13 | role1 |
| 10 | sky_2 | (0.2, 0.2, 0.2) | 否 | env_14 | role1 |

资源文件：`Resources/Mats/` 下存在 `env_0..env_14`、`sky_0..sky_4`（注意 env_2/env_3/env_11 等中间索引文件也都存在，但本游戏皮肤库只引用了上表中的索引）。`m_vMaterial_role` 至少 2 个角色材质（role0、role1）。

---

## 5. `LevelBase` 通用关卡属性（本游戏取值）

每个关卡 Prefab 根节点（即 `Level_PutMan`）继承自 `LevelBase`，本游戏所有关卡的通用字段几乎一致，复刻时统一设为：

| 字段 | 含义 | 本游戏取值 |
|---|---|---|
| `m_eLevelType` | 关卡类型枚举 | `PutMan` (=1) |
| `m_eLevelFuncType` | 普通/每日/活动 | `eNormal` (=0) |
| `m_iDiffcultDiv` | 难度除数 | 多数 `1`，L_3/L_4/L_5_old/L_6 为 `3` |
| `m_iLevelShow` | 显示关号 | 运行时由系统设为当前关号 |
| `m_iLevelDiff` | 实际难度 | `1` |
| `m_bHasTouchToStartUI` | 点屏开始 | `0`（直接可玩） |
| `m_bShowToolNum` | 显示道具数 | `0` |
| `m_bShowLvlText` | 显示关卡文字 | 运行时被强制 `false`（用进度条） |
| `m_bShowReplayBtn` | 显示重玩 | `1` |
| `m_bShowMainBtn` | 显示主页 | `0` |
| `m_bShowReturnBtn` | 显示返回 | `0` |
| `m_eCameraSettting` | 相机设置 | `eNormal` |
| `m_cameraColor` | 相机背景 | 由皮肤覆盖 |
| `m_bShowGameOverUI` | 结算 UI | `1` |
| `m_isShowBanner` | 横幅广告 | `1`（复刻可忽略广告） |
| `m_bShowLight` | 显示灯光 | `1` |
| `m_ambientLight` | 环境光强 | `45`（/255 作为灰度环境光） |
| `m_vLightAngle` | 平行光角度 | `(154.128, 167.692, 10.75)` |
| `m_iShowEdges` | 物理边界位掩码 | `15`（上右下左全开，本游戏无物理掉落可忽略） |
| `m_bShowHint` | 提示 | `0` |
| `m_strHint` | 提示文本 | `"Hello"`（未用） |

> 复刻最小实现时，只需关注：相机背景色/天空盒、平行光角度与环境光、顶部进度条与 Replay 按钮。其它（广告、道具、提示）可不实现。

---

## 6. 全部 46 个关卡数据（逐关）

### 6.1 数据说明

下表中：
- **皮肤** = `m_skinIndex`。
- **可拖小人数** = `m_iInitPutManNum`。
- **数字池** = `m_vLeft` ∪ `m_vRight`（运行时合并为可拖动小人的数字；分组仅供参考，常代表一个标准解）。
- **左站/右站** = `m_vStandInSeesawLeft` / `Right`（开局固定站立、不可拖动的小人数字）。
- **隐藏** = `m_vIsHide`（可拖小人中哪几个隐藏数字，按池顺序；T=隐藏）。站立隐藏单列说明。
- **字母模式** = `m_bUseLetter`（1=隐藏时显示字母，0=显示`?`）。
- **Buff** = 座位倍率（`L×n` 左盘某座位 ×n，`R×n` 右盘某座位 ×n）。所有 buff 都是乘法。
- **排序** = `m_bRandomTheLeftAndRight`（1=随机打乱，0=降序）。
- 目标：左盘合计 == 右盘合计（含站立小人与 buff 后）。

> 难度除数 `m_iDiffcultDiv` 仅 L_3/L_4/L_5_old/L_6 = 3，其余 = 1。
> 标注 `_old` 的是旧版备份关卡（L_5_old、L_10_old、L_15_old、L_36_old），**正式关卡序列不包含它们**，仅作历史参考。

### 6.2 正式关卡（按游玩顺序 L_1 → L_46）

| 关 | 皮肤 | 可拖数 | 数字池(vLeft / vRight) | 左站 | 右站 | 隐藏(池) | 字母 | Buff | 排序 | 备注 |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | 0 | 4 | [1,4] / [2,3] | - | - | - | 0 | - | 随机 | 新手：左右各和=5 |
| 2 | 0 | 5 | [1,6] / [1,3,3] | - | - | - | 0 | - | 随机 | 和=7 |
| 3 | 0 | 4 | [3,8] / [5,6] | - | - | - | 0 | - | 随机 | 和=11，难度除3 |
| 4 | 0 | 5 | [2,3,5] / [9,1] | - | - | - | 0 | - | 随机 | 和=10，难度除3 |
| 5 | 6 | 3 | [2] / [1,5] | - | - | - | 0 | **L×3** | 随机 | 左座×3：2×3=6=1+5 |
| 6 | 0 | 5 | [6,6] / [3,4,5] | - | - | - | 0 | - | 随机 | 和=12，难度除3 |
| 7 | 0 | 5 | [5,6] / [3,5,3] | - | - | - | 0 | - | 随机 | 和=11 |
| 8 | 0 | 5 | [11,3] / [5,7,2] | - | - | - | 0 | - | 随机 | 和=14 |
| 9 | 0 | 5 | [13] / [6,3,2,2] | - | - | - | 0 | - | 随机 | 和=13 |
| 10 | 6 | 4 | [4,4,4] / [6] | - | - | - | 0 | **R×2** | 随机 | 右座×2：6×2=12=4+4+4 |
| 11 | 0 | 6 | [6,6,6] / [9,2,7] | - | - | - | 0 | - | 随机 | 和=18 |
| 12 | 0 | 6 | [1,2,3] / [4,5,7] | - | - | - | 0 | - | **降序** | 池总=22，需各侧=11(4+7 / 1+2+3+5) |
| 13 | 0 | 6 | [10,10,10] / [15,9,6] | - | - | - | 0 | - | 随机 | 和=30 |
| 14 | 0 | 6 | [4,4,7] / [5,5,5] | - | - | - | 0 | - | 随机 | 和=15 |
| 15 | 6 | 5 | [5,7,5] / [6,6] | - | - | - | 0 | **L×0** | 随机 | 左某座×0（使该人作废） |
| 16 | 0 | 3 | [3] / [2,4] | [3] | - | - | 0 | - | 随机 | 左已站1人=3 |
| 17 | 0 | 3 | [10] / [5,6] | [3] | [4] | - | 0 | - | 随机 | 两侧各有站立配重 |
| 18 | 7 | 3 | [10] / [8,8] | [10] | [4] | - | 0 | - | 随机 | |
| 19 | 0 | 3 | [2,3] / [8] | [6,5] | [8,6] | - | 0 | - | 随机 | 两侧各站2人 |
| 20 | 8 | 4 | [5,6] / [2,4] | [10] | [20] | - | 0 | **L×2** | 随机 | 站立配重+左座×2 |
| 21 | 0 | 5 | [9,3,3] / [10,17] | [12] | - | - | 0 | - | 随机 | |
| 22 | 0 | 5 | [9,7] / [10,10,12] | [8,8] | - | - | 0 | - | 随机 | |
| 23 | 0 | 4 | [5,10] / [5,13] | [10,5] | [6,12] | - | 0 | - | 随机 | 两侧各站2人 |
| 24 | 10 | 7 | [5,15,10,10] / [8,13,19] | - | - | - | 0 | - | 随机 | 和=40，夜间皮肤 |
| 25 | 6 | 5 | [3,15] / [10,20,3] | - | - | - | 0 | **L×5, R×0** | 随机 | 左座×5、右座×0 |
| 26 | 0 | 2 | [1] / [1] | - | - | [T,F] | 0 | - | 随机 | 第1人数字为`?` |
| 27 | 0 | 3 | [5] / [2,3] | - | - | [T,F,F] | 0 | - | 随机 | |
| 28 | 0 | 4 | [10] / [5,4,1] | - | - | [F,T,F,F] | 0 | - | 随机 | |
| 29 | 0 | 4 | [4,6] / [3,7] | - | - | [F,T,F,F] | 0 | - | 随机 | |
| 30 | 6 | 4 | [1,11] / [6,6] | - | - | [T,T,F,F] | 1 | - | 随机 | 隐藏显示字母 |
| 31 | 0 | 7 | [1,3,5,7] / [6,4,6] | - | - | - | 1 | - | 随机 | 和=16 |
| 32 | 0 | 7 | [5,3,8,6] / [11,3,8] | - | - | - | 1 | - | 随机 | 和=22 |
| 33 | 0 | 7 | [4,4,12,10] / [13,8,9] | - | - | - | 1 | - | 随机 | 和=30 |
| 34 | 0 | 6 | [15,15,15] / [20,17,8] | - | - | - | 1 | - | 随机 | 和=45 |
| 35 | 6 | 5 | [2,8,2] / [3,9] | - | - | [T,F,T,F,F] | 1 | - | 随机 | 和=12 |
| 36 | 0 | 5 | [1,2,3] / [2,4] | - | - | [T,F,F,F,F] | 0 | - | 随机 | 和=6 |
| 37 | 0 | 5 | [10,5,5] / [12,8] | - | - | [T,F,F,F,F] | 0 | - | 随机 | 和=20 |
| 38 | 0 | 5 | [20,4,6] / [15,15] | - | - | [T,F,F,F,F] | 0 | - | 随机 | 和=30 |
| 39 | 0 | 6 | [50,50,10] / [45,30,35] | - | - | - | 0 | - | 随机 | 和=110，大数字 |
| 40 | 6 | 6 | [5,2,3] / [2,2,2] | - | - | - | 0 | **R×3** | 随机 | 右某座×3 |
| 41 | -1(→10) | 7 | [8,7,3] / [4,4,7,3] | - | - | [F,F,F,T,T,F,F] | 1 | - | 随机 | 和=18，夜间皮肤 |
| 42 | 0 | 5 | [1,2,3,2] / [8] | - | - | [F,F,F,F,T] | 1 | - | 随机 | 和=8 |
| 43 | 0 | 3 | [] / [2,1,2] | [9] | [4] | - | 1 | - | 随机 | 站立小人隐藏(均T)；可拖池仅右组3人 |
| 44 | 0 | 4 | [32] / [16,16,3] | [6] | [3] | [F,F,F,T] | 1 | - | 随机 | 站立均隐藏；大数字 |
| 45 | 0 | 4 | [4] / [4,4,2] | - | - | - | 1 | **L×2, R×2** | 随机 | 左右座各×2 |
| 46 | 0 | 7 | 程序随机生成 | - | - | (随机) | 0 | - | 随机 | `m_GeneRandomPutMan=1`，按 2.7 算法生成；无尽/兜底关 |

> 第 43 关特殊：`m_vLeft` 为空、`m_vRight=[2,1,2]`，`m_iInitPutManNum=3`，即 3 个可拖小人数字为 [2,1,2]；左站[9]、右站[4] 都隐藏数字（字母模式）。
> 第 41 关 `m_skinIndex=-1`，按取模规则等价于皮肤索引 10（夜间天空盒）。

### 6.3 旧版备份关（不在正式序列，仅参考）

| 文件 | 皮肤 | 可拖数 | 数字池 | 难度除 | 说明 |
|---|---|---|---|---|---|
| L_5_old | 0 | 5 | [3,1,2] / [4,2] | 3 | 旧第5关 |
| L_10_old | 0 | 5 | [6,6] / [3,4,5] | 1 | 随机生成版 |
| L_15_old | 0 | 5 | [6,6] / [3,4,5] | 1 | 随机生成版 |
| L_36_old | 0 | 7 | [10,20,12,8] / [10,16,24] | 1 | 字母模式 |

---

## 7. 角色 `MyManModule`（小人模型与动画）

挂在小人模型上，封装 `Animator m_ani` 和 `SkinnedMeshRenderer m_meshRender`（用于换肤）。

动画状态名（`m_ani.Play("...")`）本游戏用到的主要是：
- `idle`（待机）、`sit`（坐下，放到座位且 `PutMan.m_bIsSet=true` 时）、`victory`/`dancing`（胜利，可选）。
- 其余备用：`walk/running/jump/dancing/lose/...`（完整列表见原脚本，复刻时只需 `idle` 和 `sit` 即可，胜利动画可加 `victory`）。

最小复刻：一个带 `Animator` 的小人，至少有 `idle`、`sit` 两个动画；`SkinnedMeshRenderer` 暴露材质供换肤。

---

## 8. 游戏流程（与外部框架解耦后的最小闭环）

1. 启动 → 读取"当前关卡号"（用 `PlayerPrefs` 存进度即可，从 1 开始）。
2. 从 `Resources/l_MathSeesaw/L_{关号}` 加载关卡 Prefab，实例化为 `Battle` 子物体。
3. `Level_PutMan.Start()` 执行初始化（皮肤、小人、站立配重、跷跷板初始角度、进度条）。
4. 玩家拖动小人到座位；每次放置/取出后 `updateScore()` → `Blance.updateWeight()` 播放倾斜动画。
5. 倾斜动画结束回调 `checkAndDealGameOver()`：两盘相等且所有可拖小人就位 → 胜利。
6. 胜利：揭示隐藏数字、播放胜利特效；关卡号 +1 并存档；显示"下一关"。
7. 加载下一关，回到第 2 步。`L_46` 之后可循环用 `m_GeneRandomPutMan` 程序生成的无尽关。

> 原工程的胜利还串接了广告、GA、评分弹窗等（`Battle.setGameWin`），复刻时**全部可省略**，只保留"存档 + 进下一关"。

### 关卡顺序与资源
- 关卡资源路径固定为 `Resources/l_MathSeesaw/L_{n}.prefab`，n = 1..46。
- 正式游玩顺序就是 1→46（原工程用一张 udid→respath 映射表实现，复刻直接用关号即可）。
- 第 46 关之后可重复随机关。

---

## 9. 复刻检查清单（Definition of Done）

- [ ] 跷跷板：左右托盘各 4 座位；重量差→按 `getAngleByWeight` 倾斜（最大 13°，1.5s DOTween 动画）。
- [ ] 小人：可点选、跟手拖动、吸附到最近空座位、放置/取出实时更新两盘总分。
- [ ] 站立小人：开局固定在座位、锁定不可拖、计入初始重量（瞬时生效）。
- [ ] Buff 座位：放上的小人数字按 `×n` 计入（显示也变化）。
- [ ] 隐藏数字：`?`（useLetter=0）或字母 `A/B/C`（useLetter=1）；托盘总分若含隐藏项显示 `?`；胜利时全部揭示。
- [ ] 胜利判定：两盘相等 + 所有可拖小人就位。
- [ ] 11 套皮肤（背景色/天空盒/环境材质/角色材质/云开关）按第 4 节数值。
- [ ] 顶部进度条 `1-2-3-4-Boss`，当前关高亮 `#00E6FF`，5 档填充。
- [ ] 46 关数据按第 6 节逐关录入；L_46 用程序随机生成。
- [ ] 关卡推进与存档（PlayerPrefs 关号）。
- [ ] **美术（第 11 节）**：火柴人模型/动画、跷跷板横梁(HYPEPOLY)+支点+托盘+座位、云朵、buff/粒子等 3D 资产已导入。
- [ ] **材质/Shader**：`mat_Seesaw`、`env_*`（自定义 `MyDiffuse2Tex` shader）、`sky_*`、`Stickman_*` 颜色与原版一致。
- [ ] **字体**：`Roboto-Condensed` 应用于小人数字 TextMesh、托盘合计与进度条。
- [ ] **场景布局**：跷跷板/托盘/散落小人/云朵的坐标与朝向按 11.3 还原。

---

## 10. 源文件对照（原工程位置，便于核对细节）

| 模块 | 原文件 |
|---|---|
| 关卡主控/拖拽/胜负/随机生成 | `Assets/game_balance_blockAndMan/game_PutMan/Script/Level_PutMan.cs` |
| 小人 | `.../game_PutMan/Script/PutMan.cs` |
| 座位 | `.../game_PutMan/Script/ManPlace.cs` |
| 托盘 | `.../game_PutMan/Script/NumContainer_PutMan.cs` + `game_BalanceBase/NumContainer_Base.cs` |
| 跷跷板 | `.../game_Balance/Script/Blance.cs` |
| 倍率 Buff | `.../game_PutMan/Script/PutManBuff.cs` |
| 皮肤库/材质加载 | `.../Scripts_New/Common/BattleRes.cs` + `Scripts_New/MathSeesawSkin.cs` |
| 进度条 | `.../Scripts_New/LevelProgress.cs` |
| 容器引用 | `.../Scripts_New/All_MathSeesaw.cs` + `Cav_MathSeesawUI.cs` |
| 关卡通用基类 | `Assets/Script/Base/LevelBase.cs` |
| 角色动画 | `Assets/Common/MyMan/Script/MyManModule.cs` |
| 关卡资源 | `Assets/Resources/l_MathSeesaw/L_1..L_46.prefab` |
| 皮肤材质 | `Assets/Resources/Mats/env_*.mat`, `sky_*.mat` |
| 场景美术成品（见第 11 节） | `Assets/game_balance_blockAndMan/game_PutMan/prefab/AllPutMan.prefab` |
| 角色模型 | `Assets/Common/MyMan/OriginFbx_Download/Stickman_heads_sphere.fbx`、`Common/MyMan/MyMan_mathrun.prefab` |
| 跷跷板横梁（第三方） | `Assets/HYPEPOLY - Battle Royale Show/Prefabs/Static/Obstakles by parts/Balancing Platform Long_8.prefab` |
| 跷跷板材质 | `Assets/game_balance_blockAndMan/game_Balance/mat/mat_Seesaw.mat` |
| 环境 shader | `Assets/Shader/MyDiffuse2Tex.shader` |
| 云朵贴图 | `Assets/game_balance_blockAndMan/game_Balance/res/cloud.psd` |
| 字体 | `Assets/Common/Font/Roboto-Condensed.ttf` |

---

## 11. 美术风格与资产（外观复刻核心）

> 前面 1–10 节是"逻辑/数据"复刻；本节是"长什么样"的美术复刻。
> **最重要的建议先说**：本游戏的美术资产（角色、动画、跷跷板模型、材质、贴图、字体）大多是**第三方/通用资源**，强烈建议**直接把原始资产文件拷贝到新工程复用**（见 11.7 资产清单），而不是让 AI 凭空重做——这样外观能 100% 一致，新工程只需重写第 2–3 节的代码逻辑。下面的描述用于"理解风格"或"找不到原文件时近似重做"。

### 11.1 整体美术风格

- **品类观感**：休闲 3D（casual 3D），低多边形（low-poly）、扁平纯色、卡通、明亮。
- **角色**：**火柴人（Stickman）**，圆球形脑袋 + 细长四肢，纯色无贴图（不同小人用不同纯色区分）。来自 Mixamo 风格人形。
- **场景**：一个放在浅色"舞台"上的**跷跷板/天平**。背景是**纯色 pastel 天空**（每个皮肤一个颜色，见第 4 节）+ 几朵**缓慢横向飘动的白色云朵 sprite**。
- **相机**：透视相机，略微俯视看向跷跷板正面。背景用纯色（多数皮肤）或天空盒（夜间皮肤 8/9/10）。
- **光照**：单个平行光（角度 `(154.128, 167.692, 10.75)`）+ 低强度灰度环境光（`45/255 ≈ 0.176` 灰），整体柔和无强阴影。
- **数字呈现**：每个小人身上用 **3D TextMesh** 直接显示数字（字体 Roboto-Condensed，黑色）；托盘上方也有一个 TextMesh 显示该侧合计。
- **氛围**：干净、扁平、低饱和但明快；没有写实材质，金属/高光很弱。

### 11.2 角色（小人）美术

- **模型来源**：`Common/MyMan/OriginFbx_Download/Stickman_heads_sphere.fbx`（圆球头火柴人，Mixamo 体系）。
- **运行用预制**：场景里散落的小人是 `game_balance_blockAndMan/game_PutMan/prefab/PutMan.prefab`，其内部包含角色模型预制 `Common/MyMan/MyMan_mathrun.prefab`（火柴人 + 动画）。
- **动画**：Mixamo `.fbx` 动画片段，由 `Common/MyMan/Ani/MyMan.controller` 驱动；本游戏主要用 `idle`（待机）和 `sit`（坐到座位）；可选 `victory`。
- **材质**：Unity **Standard shader** 的纯色材质（无贴图），低金属低高光。换肤时由皮肤系统替换：
  - `role0` = `Common/MyMan_3/Materials/Stickman_Green.mat`，颜色绿 `(0, 0.689, 0.161)`。
  - `role1` = 夜间皮肤（8/9/10）用的另一套角色材质（深色/对比色）。
  - 散落小人本身的预制还自带多种 `Stickman_*.mat`（如 `Stickman_Gray` 蓝 `(0,0.6875,1)`、`Stickman_Yellow` 橙 `(1,0.66,0)` 等，见 `Common/MyMan/Materials/`），用于让不同小人颜色不同。
- **数字子物体**：小人下挂一个名为 `num` 的 TextMesh（字体 Roboto-Condensed），跟随 `PutMan.updateNum()` 改文字与缩放（数值越大字越小，见 2.1）。

### 11.3 跷跷板 / 场景道具美术（`AllPutMan.prefab`）

`AllPutMan.prefab`（`game_balance_blockAndMan/game_PutMan/prefab/AllPutMan.prefab`）是整套可见美术的成品，结构与关键变换如下（本地坐标）：

| 部件 | 说明 | 模型 / 材质 | 变换（local） |
|---|---|---|---|
| `Balance` | 跷跷板根 | — | pos `(0, -0.299, 1.248)`，scale `(0.8, 0.8, 1)` |
| └ `tri_zhu` | 三角形支点柱 | mesh 来自 `Res3D_Prefab/all.FBX` 子网格；材质 `mat_Seesaw` | rot 使其成三角，scale ≈ 7 |
| └ `Cube` | 支点底座 | Unity Cube + `mat_Seesaw` | 扁平方块 |
| └ `upCompent` | **会绕 Z 轴倾斜的横梁根**（Blance.m_upComponent） | — | pos `(0, -0.002, 0)` |
| &nbsp;&nbsp;└ `frame/Balancing Platform Long_8` | 横梁（长平台） | **HYPEPOLY 资产包**：`HYPEPOLY - Battle Royale Show/Prefabs/Static/Obstakles by parts/Balancing Platform Long_8.prefab` | 横置 |
| &nbsp;&nbsp;└ `leftContainer` | 左托盘（NumContainer + 4 座位 + 合计 TextMesh） | 预制 `aceb7057…`（=托盘预制） | local `(-0.2528, 0.421, -0.01)` |
| &nbsp;&nbsp;└ `rightContainer` | 右托盘 | 同上 | local `(0.34, 0.421, -0.01)` |
| `Cube`（顶层） | 地面/舞台 cube（皮肤 env 材质贴在它上面） | Unity Cube + 皮肤 `env_*` 材质 | pos `(-0.0001, -0.179, -0.034)`，scale `(0.1538, 0.079, 0.1196)` |
| `Cube`（前景台） | 小人散落的前景台 | Unity Cube + 材质 `a82eeeec…` | pos `(-0.053, -0.743, -2.21)`，scale 较大 |
| `putMans` | 8 个 `PutMan` 实例容器 | 8×`PutMan.prefab` | 散落在 y≈`-0.569`、z≈`-1.498`，x 从 `-0.371` 到 `0.379`，朝向摄像机 |
| `Clouds` | 装饰云（3–4 个） | sprite `cloud.psd` + `MovePoints` 脚本左右飘 | speed `0.05`，云在 z≈`4.33` 远处 |
| `bg_h` / `bg_v` | 拖拽用的不可见碰撞平面（Layer 9） | Quad mesh，MeshRenderer **关闭**，仅 MeshCollider | 覆盖操作区 |
| `Cav_MathSeesawUI` | 世界/叠加 UI（进度条挂这里） | 预制 `a33b081150…` | 顶部 |

要点：
- 跷跷板**横梁是现成的 HYPEPOLY 低多边形平台模型**；支点是另一个 FBX 的三角网格；底座/前景是简单 Cube。
- **倾斜动画**只旋转 `upCompent`（绕 Z），不动支点与底座（见 2.4）。
- 散落小人都朝向摄像机（统一旋转四元数约 `(0.031, -0.612, 0.787, 0.069)`）。
- 云朵用 `MovePoints` 脚本在两点间循环平移制造飘动感。

### 11.4 跷跷板 / 环境材质

| 材质 | 文件 | shader | 关键参数 |
|---|---|---|---|
| 跷跷板主体（支点/底座/横梁） | `game_balance_blockAndMan/game_Balance/mat/mat_Seesaw.mat` | Unity Standard | 颜色粉紫 `(1, 0.486, 1)`，Metallic `1`，Glossiness `0.146` |
| 前景台 | 材质 guid `a82eeeec…` | Standard | 纯色 |
| 环境地台/天空感 `env_0..14` | `Resources/Mats/env_*.mat` | **自定义 `Shader/MyDiffuse2Tex.shader`**（双纹理漫反射卡通 shader） | env_0：`_MainTex = Res/Quad_8x8.png`（棋盘格）、`_MainTex2`（细节图，平铺 4×2.5）、`_Tex1Color` 黄 `(0.934, 0.865, 0)`、`_Shininess 0.03` |
| 天空盒 `sky_0..4` | `Resources/Mats/sky_*.mat` | 天空盒 shader | 夜间皮肤 8/9/10 使用 `sky_2 / sky_3` |
| 角色材质 | `Common/MyMan*/Materials/Stickman_*.mat` | Standard | 纯色（见 11.2） |
| 半透明落点小人 | `game_balance_blockAndMan/game_PutMan/mat/manAlpha.mat` | 半透明 | 拖拽提示用 |

> 注意：环境用的是**自定义 shader `MyDiffuse2Tex`**（双纹理混合 + 自定义着色），复刻时若没有该 shader，外观会不同——建议直接复用 `Shader/MyDiffuse2Tex.shader` 与其依赖贴图（如 `Res/Quad_8x8.png`）。

### 11.5 字体与 UI

- **字体**：`Common/Font/Roboto-Condensed.ttf`（小人数字 TextMesh、托盘合计、关卡文字都用它；进度条用其 TMP 版本）。
- **进度条**：`game_balance_blockAndMan/Prefabs_New/LevelProgress.prefab`（脚本见第 3 节），样式 `1-2-3-4-Boss`，当前关高亮色 `#00E6FF`，5 档填充 `{0, 0.121, 0.322, 0.523, 0.724}`。
- **UI 容器**：`Prefabs_New/Cav_MathSeesawUI.prefab`；边框图素材 `Prefabs_New/Frame_ListFrame07_s.prefab`、`Frame_ListFrame11_Purple.prefab`。
- **顶部战斗 UI**（关卡文字 / Replay 按钮）由通用 `Canvas_Battle` 提供（原工程在主场景，复刻时自建一个简单 Canvas 即可）。

### 11.6 特效与音效

- **粒子/特效**：`game_balance_blockAndMan/game_PutMan/prefab/PortalDust.prefab`（尘埃）、`game_PutMan/MagicZonePink.prefab`（粉色魔法区，buff 座位氛围）、座位 buff 标识 `Buff.prefab`。胜利特效由通用 `Battle.showWinEffect` 触发（`m_iWinEffectIndex` 选择），复刻时可用任意庆祝粒子替代。
- **音效**：本游戏脚本里主要是**触觉反馈** `TapticManager.Impact(...)`（点选/放置时轻震动）；音效走通用框架，复刻可自行加点击/胜利音效（非必需）。

### 11.7 美术资产复用清单（推荐：直接拷贝到新工程）

按依赖整组拷贝以下文件夹/文件，外观即可 1:1 还原：

| 用途 | 路径 |
|---|---|
| 角色模型/动画/控制器/材质 | `Assets/Common/MyMan/`、`Assets/Common/MyMan_3/`（含 `OriginFbx_Download/Stickman_heads_sphere.fbx`、`Ani/*.fbx`、`MyMan.controller`、`Materials/Stickman_*.mat`） |
| 字体 | `Assets/Common/Font/Roboto-Condensed.ttf` |
| 游戏美术成品（跷跷板/小人/托盘/座位/云/buff/粒子/材质） | `Assets/game_balance_blockAndMan/`（重点：`game_PutMan/prefab/AllPutMan.prefab`、`PutMan.prefab`、`ManPlace.prefab`、`Buff.prefab`、`PortalDust.prefab`、`MagicZonePink.prefab`；`game_Balance/mat/mat_Seesaw.mat`、`game_Balance/res/cloud.psd`；`Prefabs_New/LevelProgress.prefab`、`Cav_MathSeesawUI.prefab`、`Frame_*`） |
| 跷跷板横梁模型（第三方） | `Assets/HYPEPOLY - Battle Royale Show/Prefabs/Static/Obstakles by parts/Balancing Platform Long_8.prefab`（及其依赖网格/材质） |
| 支点等通用 3D | `Assets/Res3D_Prefab/all.FBX` |
| 环境/天空材质 + 依赖贴图 | `Assets/Resources/Mats/env_*.mat`、`sky_*.mat`，以及它们引用的贴图（如 `Assets/Res/Quad_8x8.png`） |
| 自定义环境 shader | `Assets/Shader/MyDiffuse2Tex.shader` |
| 关卡 | `Assets/Resources/l_MathSeesaw/L_1..L_46.prefab`（已含全部美术引用，直接拷过去最省事） |

> 拷贝时连同每个文件的 `.meta` 一起拷，保持 guid 不变，引用关系才不会断。
> 若关卡 prefab 一起拷过去，则第 6 节的数据其实已经"内置"在 prefab 里，新工程只需重写第 2–3 节的脚本（脚本 guid 要与 prefab 里引用的一致，或重新挂载）。

### 11.8 美术近似重做指引（找不到原文件时）

若无法拿到原始资产，按下面要点能做出"神似"的版本：
1. 角色：任意圆头火柴人 / 简单胶囊人，纯色 Standard 材质，配 `idle`+`sit` 动画。
2. 跷跷板：一根长方体横梁 + 三角/梯形支点 + 扁平底座；横梁绕中心 Z 轴旋转，最大 ±13°。
3. 托盘：横梁两端各一个浅盘 + 4 个圆柱座位 + 一个 3D 数字文字。
4. 背景：纯色天空（用第 4 节颜色表）+ 2–4 个白色云 sprite 横向缓慢平移。
5. 光照：一盏柔和平行光 + 低环境光；无强阴影。
6. 字体：Roboto Condensed 或任意几何无衬线字体，黑色数字。
7. 配色：低多边形扁平、明快 pastel；buff 座位用粉色光圈点缀。

---

> 备注：所有关卡数组在 prefab 里以**小端 int32 十六进制**存储（如 `0100000004000000` = `[1,4]`），bool 数组每字节 `00/01`。本文档已全部解码为十进制，可直接录入。
